DATABASE_URL="postgresql://user:password@localhost:5432/nutri_nunes"
JWT_SECRET="troque-por-uma-chave-segura-de-32-caracteres"

# Slug do tenant para desenvolvimento local
TENANT_SLUG_DEV="adriana"

# Slug padrão para redirect da raiz em produção (path-based)
# Remover quando migrar para domínio próprio com wildcard
DEFAULT_TENANT_SLUG="adriana"

# Email (Resend.com) — opcional, se não configurar os emails são logados no console
RESEND_API_KEY="re_xxxxxxxxxxxx"
FROM_EMAIL="Adriana Nutrição <noreply@seudominio.com>"
